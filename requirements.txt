# Pins verified on macOS 26.2 arm64 (M3 Pro).
# mediapipe 1.0.1 SIGABRTs on Apple Silicon at graph open (issue #6356) — do not bump.
mediapipe==1.0.0
# mediapipe pulls opencv-contrib-python; 5.x is a breaking major. Never also install opencv-python.
opencv-contrib-python>=4.10,<5
numpy>=2
pynput>=1.7
pyarrow>=17
scipy>=1.14
matplotlib>=3.9
pytest>=8

# Recognition, decoding and experiments (versions as used; macOS arm64).
torch>=2.14
transformers>=5.17
lightgbm>=4.7
scikit-learn>=1.9
joblib>=1.6
pillow>=12
zeroconf>=0.151
# Optional: the 8B decoder runs through MLX on Apple Silicon (installed in its own venv, see phase0/analysis/llmdec_mlx.py).
# mlx, mlx-lm
# Optional: live demo speech labels and experiments.
# sounddevice, openai-whisper, rtmlib, onnxruntime
